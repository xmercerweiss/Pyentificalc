[project]
name = "pyentificalc"
version = "0.1"
dependencies = [
    "pytest>=8.4.0"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.ruff] 
line-length = 120
ignore = [
    "F401"
]
